# 3.2.1. Báo cáo kết quả chức năng (Functional Results)

## 1. Tổng quan đợt kiểm thử
- **Thời gian chạy**: Tự động qua CI/CD Pipeline (Jest & Playwright).
- **Tổng số Test Cases cốt lõi**: 15 (Đại diện cho hàng trăm kịch bản con).
- **Tỷ lệ Đạt (Pass Rate)**: **100%** (15/15 Passed).
- **Tiêu chuẩn Release**: Hoàn toàn đáp ứng điều kiện Release nội bộ. Các lỗi (Defects) phát hiện trong lần chạy baseline trước (đặc biệt là 400 Bad Request của module AI) đã được phân tích nguyên nhân tận gốc (do lỗi format mock data trên môi trường test) và khắc phục thành công.

## 2. Kết quả chi tiết theo Module

### 2.1. Module Authentication & Authorization (100% Pass)
| ID | Tên kịch bản | Trạng thái | Ghi chú |
|---|---|---|---|
| TC-AUTH-01 | Đăng nhập thành công với thông tin đúng | **PASS** | Token trả về hợp lệ, mã hóa JWT an toàn. |
| TC-AUTH-02 | Sai password trả về 401 | **PASS** | |
| TC-AUTH-03 | Email không tồn tại trả về 401 | **PASS** | |
| TC-AUTH-05 | Request body thiếu/sai định dạng trả về 400 | **PASS** | Chặn bởi Zod Validation. |
| TC-RBAC-01 | ADMIN có quyền truy cập Users list | **PASS** | |
| TC-RBAC-04 | SALES_STAFF bị chặn truy cập Users list (403) | **PASS** | Middleware phân quyền hoạt động tốt. |
| TC-RBAC-05 | SALES_STAFF bị chặn truy cập cài đặt hệ thống (403)| **PASS** | |

### 2.2. Module Quản lý Kho (Inventory) (100% Pass)
| ID | Tên kịch bản | Trạng thái | Ghi chú |
|---|---|---|---|
| TC-INV-01 | Nhập kho thành công | **PASS** | Tồn kho tăng chuẩn xác. |
| TC-INV-02 | Xuất kho thành công sau khi đã có tồn | **PASS** | Tồn kho giảm tương ứng. |
| TC-INV-04 | Xuất kho thất bại nếu số lượng trong kho bằng 0 | **PASS** | Chặn lỗi âm kho tuyệt đối. |

### 2.3. Module Bán hàng (Sales) (100% Pass)
| ID | Tên kịch bản | Trạng thái | Ghi chú |
|---|---|---|---|
| TC-SALE-01 | Bán hàng hợp lệ tự động trừ tồn kho | **PASS** | Hoạt động trơn tru. |
| TC-SALE-03 | Rollback toàn bộ Order khi có sản phẩm không đủ Stock| **PASS** | Đảm bảo tính ACID của CSDL. API trả về 400 đúng chuẩn. |

### 2.4. Module AI Insight (100% Pass)
| ID | Tên kịch bản | Trạng thái | Ghi chú |
|---|---|---|---|
| TC-AI-14 | Fetch latest analysis thành công | **PASS** | API trả về 200 OK. |
| TC-AIP-01| Tạo kế hoạch nhập hàng loạt (Bulk Apply) | **PASS** | Đã sửa dứt điểm lỗi giả lập DB ở đợt chạy trước. Tạo thành công phiếu nháp. |
| TC-AIP-02| Chặn thao tác Bulk Apply trùng lặp (Duplicate Apply) | **PASS** | Hệ thống bảo vệ tính toàn vẹn bằng cách trả về 400 nếu gợi ý đã được xử lý. |

## 3. Kết luận
Các luồng nghiệp vụ xương sống bao gồm Phân quyền, Tồn kho (ngăn chặn xuất âm) và Bán hàng (Rollback khi thiếu hàng) đều hoạt động chuẩn xác và hoàn hảo. Hệ thống sẵn sàng để triển khai và bàn giao cho người dùng cuối.
