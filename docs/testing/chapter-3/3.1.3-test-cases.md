# 3.1.3. Kịch bản và Ca kiểm thử (Test Cases)

Dưới đây là ma trận các ca kiểm thử (Test Cases) trọng yếu đại diện cho các tính năng hệ thống, được thiết kế để đo lường tự động thông qua Vitest và Playwright.

| ID | Chức năng | Mục tiêu | Tiền điều kiện | Các bước | Dữ liệu Test | Kết quả mong đợi | Kết quả thực tế | Trạng thái |
|---|---|---|---|---|---|---|---|---|
| **TC-AUTH-01** | Đăng nhập | Đăng nhập thành công với tài khoản đúng | Tài khoản tồn tại trong DB | 1. Gọi API POST `/auth/login` với email/pass đúng. 2. Kiểm tra Response. | `email`, `password` hợp lệ | Trả về Status 200, Token và User info. | Chưa chạy | NOT TESTED |
| **TC-AUTH-01** | Đăng nhập | Đăng nhập thành công với tài khoản đúng | Tài khoản tồn tại trong DB | 1. Gọi API POST `/auth/login` với email/pass đúng. 2. Kiểm tra Response. | `email`, `password` hợp lệ | Trả về Status 200, Token và User info. | Đã chạy | PASS |
| **TC-AUTH-02** | Đăng nhập | Ngăn chặn đăng nhập khi sai Pass | Tài khoản tồn tại | 1. Gọi API POST `/auth/login` với pass sai. | `email` đúng, `password` sai | Trả về Status 401 Unauthorized. | Đã chạy | PASS |
| **TC-RBAC-01** | Phân quyền | Ngăn chặn Sales truy cập chức năng Admin | Tài khoản Sales tồn tại | 1. Login bằng tài khoản Sales. 2. Fetch danh sách Users. | Token của `SALES_STAFF` | Trả về Status 403 Forbidden. | Đã chạy | PASS |
| **TC-INV-01** | Nhập kho | Tăng đúng Tồn kho khi tạo phiếu nhập | Sản phẩm TEST_ tồn tại | 1. POST tạo Import Plan (Item: qty=10). | `product_id`, `qty: 10` | `stock_quantity` tăng 10, sinh ra bản ghi `IMPORT` trong `stock_movements`. | Đã chạy | PASS |
| **TC-INV-04** | Xuất kho | Chặn xuất kho nếu số lượng trong kho = 0 | Sản phẩm TEST_ có stock = 0 | 1. POST tạo phiếu Xuất kho (qty=1). | `product_id`, `qty: 1` | Trả về lỗi 400 (Bad Request), không cho xuất, tồn kho vẫn là 0. | Đã chạy | PASS |
| **TC-SALE-01** | Bán hàng | Trừ tồn kho tự động khi tạo Order | Sản phẩm TEST_ có stock = 10 | 1. POST tạo Order với sản phẩm, `qty=2`. | `product_id`, `qty: 2` | Tồn kho giảm xuống 8, sinh ra bản ghi `SALE` trong `stock_movements`. | Đã chạy | PASS |
| **TC-SALE-03** | Bán hàng | Rollback Order khi không đủ Stock | Sản phẩm TEST_ có stock = 1 | 1. POST tạo Order với 2 sản phẩm (sp1 đủ, sp2 thiếu). | 2 Items | Trả về 400. Toàn bộ Order hủy, SP1 KHÔNG bị trừ nhầm tồn kho. | Đã chạy | PASS |
| **TC-AI-01** | AI Forecast | Baseline Forecast hoạt động | Dữ liệu Sales mẫu có sẵn | 1. Gọi GET AI Forecast. | | Trả về dự báo Baseline không bị crash (Dù Gemini bị lỗi hoặc tắt). | Đã chạy | PASS |
| **TC-AI-14** | AI Insight | Analysis lưu và lấy lại thành công | Đã cấu hình Supabase | 1. POST chạy Run. 2. Fetch Latest Run. | | Lấy được dữ liệu Run mới nhất với `run_id`, không trả về 404. | Đã chạy | PASS |
| **TC-AIP-01** | AI Import Plan | Tạo Kế hoạch Nhập từ AI | Đã có Recommendation `PENDING` | 1. Gọi API `applyBulkRecommendations`. | `run_id` hợp lệ | `import_plans` tạo thành công. Recommendation chuyển thành `APPLIED`. | Đã chạy | PASS |
| **TC-AIP-13** | AI Import Plan | Confirm Plan cập nhật Stock và Status | Có bản nháp Import Plan từ AI | 1. Update Plan status thành `COMPLETED`. | `plan_id` | Tồn kho tăng, Recommendation chuyển từ `APPLIED` thành `COMPLETED`. | Đã chạy | PASS |

*(Lưu ý: Bảng này chỉ liệt kê các Case Cốt lõi (Core Business). Toàn bộ danh sách hàng trăm Case chi tiết sẽ được tự động báo cáo thông qua công cụ Report của Playwright và Vitest sau khi chạy execution).*
